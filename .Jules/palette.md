## 2024-05-18 - Missing ARIA Labels on Icon Buttons
**Learning:** Found multiple instances where icon-only buttons (`Trash2`, `X`) are missing `aria-label` attributes. This makes them inaccessible to screen readers.
**Action:** Will add `aria-label` to these components to improve accessibility.
